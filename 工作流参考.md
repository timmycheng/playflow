# workflow.yaml 工作流参考手册

用 YAML 像写 GitHub Actions 一样编排任务自动化。**页面结构再复杂，也只改 YAML、不改代码**。

---

## 一、快速开始

```bat
:: 菜单方式（推荐先探测/录制，再执行）
tool_kit.exe            :: 选 1 probe 探测页面 / 选 2 record 录制 / 选 3 执行 / 选 4 校验

:: 命令行方式
tool_kit.exe --probe                             :: 交互探测（可选 --workflow 复用其登录）
tool_kit.exe --record out.yaml [--url 网址]       :: 录制操作，直接生成 out.yaml
tool_kit.exe --workflow workflow.yaml            :: 执行
tool_kit.exe --workflow workflow.yaml --validate :: 只校验，不执行
```

校验会检查：YAML 语法、`uses` 动作名是否存在、步骤结构与常见参数、`tasks.mode`，并给出中文错误。

> **config.json 是可选的**：workflow 模式下 `login`、`settings` 都能写在 YAML 里，
> 同目录即使没有 `config.json` 也能直接运行；config.json 只作为没写到的项的兜底。

### 从零生成一个流程（推荐工作流）

1. `tool_kit --probe`：登录后把页面点开，每停一页按回车看元素清单；
   输入 `w=页面.yaml` 会把 `probe`（原始清单）+ `suggested_steps`（步骤草稿）存下来。
2. `tool_kit --record out.yaml`：在浏览器里把一次操作完整点一遍（点击/填写/下拉/勾选/上传/回车），
   结束后生成可直接运行的 YAML（密码自动写成 `{{ env.password }}`，不落明文）。
3. 把草稿粘进 `workflow.yaml` 的 `steps` / `tasks[].steps`，补上真实 URL 与账号，
   `--validate` 通过后先 `max_tasks: 1` 试跑。

`probe` 和 `record` 本身也是动作，可以写在流程里（比如登录后先探测当前页）：

```yaml
steps:
  - uses: goto
    with: { url: "http://内网IP/home" }
  - uses: probe                  # 省略 file = 自动写 shots/probe_*.yaml；"" = 只看控制台
    with: { all_pages: true, steps: true }
  - uses: record                 # 录制人工操作，生成 YAML；file 指定输出路径
    with: { file: "shots/我的操作.yaml" }
```

---

## 二、文件结构（顶层字段）

```yaml
name: 流程名称                  # 显示用

settings:                       # 引擎设置（可省略，省略时取 config.json 的值）
  max_tasks: 1                  # 每个类别处理条数（先 1 试跑）
  attachment_dir: 附件
  download_attachments: true
  task_delay_seconds: [1, 3]
  screenshot: true
  dialog: accept                # 原生弹窗策略：accept（默认）/ dismiss；prompt 可配 dialog_text
  sign_keywords: [签收, 受理, 确认]
  submit_keywords: [提交, 确定, 保存]
  default_radio: 同意
  radio_rules:
    - { if_type_contains: 报修, then_pick: 同意 }
    - { if_type_contains: 申请, then_pick: 驳回 }

browser:                        # 可省略
  channel: chrome
  headless: false

login:                          # 见第四节；不写则回落到 config.json 内置登录
env:                            # 自定义变量
  operator: 自动处理

steps:                          # 主流程：登录后、逐类别之前执行一次
  - uses: log
    with: { message: "操作人 {{ operator }}" }

tasks:                          # 任务类别：工具自动"取第一行→跑steps→回列表"循环
  - name: 类别一
    url: http://内网IP/list?type=1
    row_selector: ""            # 可省略，自动探测
    link_selector: ""           # 可省略，自动探测行内第一个 <a>
    max_tasks: 5                # 可覆盖 settings.max_tasks
    mode: first_row             # first_row（默认）/ once / each_row
    remove_after: true          # each_row 时：行会消失用 true（默认），行不消失用 false
    steps:
      - uses: click_row_link
      - uses: click_text
        with: { text: [签收, 受理], optional: true }
      - uses: pick_radio_by_rule
      - uses: download
      - uses: click_text
        with: { text: [提交, 确定] }
      - uses: close_task_page
```

### tasks 的三种模式（`mode`）

| mode | 行为 | 适用 |
|---|---|---|
| `first_row`（默认） | 反复取列表**第一行** → 跑 steps → 回列表，直到列表空或达到 `max_tasks`；同一任务号重复出现会终止防死循环 | 处理完的行会消失的待办队列 |
| `once` | 不找列表行，steps **只执行一次** | 单页流程、报表导出、纯接口步骤 |
| `each_row` | 按行号依次处理：`remove_after: true`（默认）= 始终取第一行（行会消失）；`false` = 依次取第 0、1、2… 行 | 行不消失的表格批量处理 |

`each_row` 循环内可用 `{{ row_index }}` 取当前行号（0 起）。
`once` 模式下 `row_text` / `task_no` 为空字符串。

### 步骤的通用字段

| 字段 | 说明 |
|---|---|
| `uses` | **必填**，动作名（见第五节） |
| `name` | 步骤名，出现在日志里 |
| `with` | 动作参数（一个映射） |
| `if` | 条件，不满足则跳过本步（见第三节） |
| `id` | 把本步返回值存入变量，之后可用 `{{ steps.<id> }}` 引用 |
| `continue_on_error` | `true` 时本步失败也不中断任务 |
| `then` / `else` | 仅 `uses: if` 用 |
| `do` | 仅循环动作用（`for_each` / `while` / `repeat` 的循环体） |

---

## 三、变量与条件

### 变量

- `{{ 名字 }}` 引用变量，支持点路径：`{{ row.no }}`、`{{ steps.picked }}`
- 内置变量：`task_no`（当前任务号）、`row_text`（当前列表行文本）、`category`（类别名）、
  `list_url`、`index`（循环下标）、`item`（循环当前项）、`settings`（引擎设置）
- `env:` 下可自定义
- 整串是单个模板时会保留原类型（`"{{ max }}"` → 数字）；嵌入文本时转成字符串

### 条件写法（`if:`）

```yaml
if: "n == 3"                              # 比较：== != > < >= <=
if: "row_text contains 报修"               # 文本包含（not contains 取反）
if: "row_text matches ^T\\d+"              # 正则
if: "s startswith 设备"                    # 前缀 / endswith 后缀
if: "type in ['报修','故障']"               # 属于
if: "a and b"      /  if: "a or b"        # 与 / 或
if: "not exists(#ok)"                     # 非
if: "count(table tr) > 3"                 # 函数可参与比较
if: "page_count() > 1"
```

**函数**（注意要带括号）：

| 函数 | 含义 |
|---|---|
| `exists(选择器)` | 选择器在任意 frame 里有命中 |
| `visible(选择器)` | 有可见命中 |
| `absent(选择器)` | 无命中 |
| `has_text(文字)` | 页面（任意 frame）正文包含该文字 |
| `count(选择器)` | 命中数量，可参与比较：`count(table tr) > 3` |
| `text(选择器)` | 第一个命中的 innerText |
| `attr(选择器, 属性名)` | 属性值 |
| `value(选择器)` | 输入框当前值 |
| `page_count()` | 当前标签页数量 |
| `url()` / `title()` | 当前页 URL / 标题 |

结构化写法（等价，便于 YAML 里书写）：

```yaml
if:
  and:
    - contains: [ "{{ row_text }}", 报修 ]
    - exists: "#signBtn"
if: { or: [ { exists: "#a" }, { exists: "#b" } ] }
if: { not: { exists: "#close" } }
```

---

## 四、登录配置

标准 SSO（账密 + UKey 人工）用通用写法即可：

```yaml
login:
  url: http://SSO地址/login
  username: 账号
  password: 密码
  username_selector: "input[name='username']"   # 可省略，自动找第一个文本框
  password_selector: "input[name='password']"   # 可省略
  button_text: [登录, 登 录]                     # 登录按钮关键词（容错空格）
  wait_after_login_ms: 500                       # 点登录后等 JS 跳转
  ukey_wait: true                                # true=暂停等人工完成 UKey
  success_url: http://内网IP/home                # 登录后访问它校验并保存 state.json
  state_file: state.json                         # 可选：登录态文件；多流程可各用各的互不干扰
```

**登录页很怪时，整段自定义：**

```yaml
login:
  steps:
    - uses: goto
      with: { url: "http://SSO地址/login" }
    - uses: fill
      with: { placeholder: 用户名, text: "zhangsan" }
    - uses: fill
      with: { selector: "#pwd", text: "{{ env.password }}" }
    - uses: click_text
      with: { text: 登录 }
    - uses: pause                    # 必停：人工完成 UKey 弹窗后回车
      with: { message: "请完成 UKey 验证" }
    - uses: wait_for
      with: { selector: "text=任务平台", timeout: 30000 }
    - uses: goto
      with: { url: "http://内网IP/home" }
```

登录成功后自动 `storage_state` 存到 `state.json`；下次运行先复用并验证，**有效期内不再触碰 UKey**。
批量中途被踢回登录页，工具会自动暂停等 UKey 后恢复队列。

> **登录态按站点校验**：复用前会检查 state 文件里的 cookie/localStorage 是否属于本流程的
> `success_url` 站点（并参考 `state.json.meta.json` 记录的来源）。若这份 state 来自别的
> 平台/别的流程，会被自动忽略并重新登录，**不会误判为已登录而跳过 UKey**。
> 多个流程想同时保留各自的登录态，给每个流程设置不同的 `login.state_file` 即可。

---

## 五、动作速查（`uses` 全部取值）

### 页面导航 / 页面管理
| 动作 | 参数 | 说明 |
|---|---|---|
| `goto` | `url`, `new_tab`(可选) | 打开网址；`new_tab: true` 在新标签页打开 |
| `reload` | — | 刷新当前页 |
| `wait` | `ms` 或 `seconds` | 等待 |
| `wait_for` | `selector`, `state`, `timeout`, `frame` | 等元素出现 |
| `wait_for_url` | `pattern`, `regex`(可选), `timeout` | 等 URL 变化（glob 或正则） |
| `switch_page` | `index` / `url_contains` / `title_contains` | 切换当前标签页 |
| `close_page` | `which: current\|others\|task` | 关页面 |
| `close_task_page` | — | 关掉任务标签页回到列表 |
| `save_state` | `path`(可选) | 把当前登录态存成 state.json（默认路径） |

### 点击 / 输入
| 动作 | 参数 | 说明 |
|---|---|---|
| `click` | `selector`, `nth`, `frame`, `capture_new_page` | 精确选择器点击 |
| `click_text` | `text`（关键词或数组）, `optional`, `capture_new_page` | **中文文字匹配**点击（容错空格、遍历 iframe） |
| `click_row_link` | — | 点当前列表行任务链接（新标签/同页自动兼容） |
| `fill` | `selector` 或 `placeholder`/`label`/`name`, `text`, `secret` | 填输入框 |
| `check` | `selector`, `nth` | 勾选复选框/单选框 |
| `pick_radio` | `text` 或 `selector` | 按文字/选择器选单选框 |
| `pick_radio_by_rule` | `type_text`（可选） | **按 settings.radio_rules 自动选** |
| `select_option` | `selector`, `label`/`value`/`option` | 下拉选择 |
| `press` | `key`, `selector`（可选） | 按键（Enter 等） |
| `hover` | `selector` 或 `text` | 悬停 |
| `scroll` | `selector` 或 `by: [x,y]` 或 `bottom: true` | 滚动 |
| `upload` | `selector`, `file`（数组可传多个） | 上传文件（相对路径锚定工具目录） |

### 数据 / 接口
| 动作 | 参数 | 说明 |
|---|---|---|
| `extract` | `selector`, `what: text\|html\|value\|attr\|href\|count`, `attr`, `all`, `join`, `name`, `default`, `frame` | 从页面取值存变量；`all: true` 取列表 |
| `evaluate` | `js`/`code`, `name`, `frame` | 执行页面 JS，返回值存变量 |
| `request` / `http` | `url`, `method`, `params`, `headers`, `json`, `data`, `timeout`, `output`, `name` | 发 HTTP 请求；结果（status/text/json/headers）存变量，`output` 可直接落盘 |

### 下载 / 校验 / 工具
| 动作 | 参数 | 说明 |
|---|---|---|
| `download` | `selector`/`text`, `dir`, `subdir`, `filename_prefix`, `optional` | 下载附件，自动归档 |
| `expect_text` | `text` | 断言页面含文字 |
| `expect_visible` | `selector`, `frame` | 断言元素可见 |
| `expect_url` | `pattern`, `regex`(可选) | 断言当前 URL 匹配 |
| `assert` | `condition`, `message` | 通用断言 |
| `screenshot` | `name` | 手动截图 |
| `log` | `message` | 打日志 |
| `pause` | `message` | 暂停等人工回车（UKey 等） |
| `fail` | `message` | 主动让当前任务失败 |
| `set_dialog` | `mode: accept\|dismiss`, `text` | 运行时改原生弹窗策略 |
| `probe` | `file`, `all_pages`, `max`, `steps` | **页面探测**：打印元素清单+选择器建议，写 YAML 草稿（省略 file 自动写 shots/） |
| `record` | `file`, `message` | **操作录制**：人工操作后回车，生成 YAML 步骤（密码脱敏为 `{{ env.password }}`） |

### 变量 / 流程控制
| 动作 | 参数 | 说明 |
|---|---|---|
| `set_var` | `name`, `value` | 设变量 |
| `parse_var` | `from`/`selector`, `regex`, `group`, `name`, `default` | 正则抽变量 |
| `if` | `condition` + `then`/`else` | 分支 |
| `for_each` | `over`/`list`/`rows`/`range`, `as`, `frame` + `do` | 遍历 |
| `while` | `condition`, `max`, `as` + `do` | 条件循环 |
| `repeat` | `times` + `do` | 重复 N 次 |
| `break` / `continue` | — | 跳出 / 继续当前循环 |
| `run_steps` | + `do` | 内联执行子步骤 |

---

## 六、应对复杂页面的常用手法

1. **按钮文字诡异** → 用 `click_text`，关键词数组多给几个近义词，自动忽略空格换行。
2. **内容在 iframe 里** → 高层动作默认遍历所有 frame，无需处理；要精确定位时用
   `frame: "#1"`（序号）或 `frame: "list2"`（URL 片段）或 `frame: main`。
3. **点链接会弹新标签页** → 用 `click_row_link`，或 `click` 加 `capture_new_page: true`。
4. **列表是 AJAX 渲染 / 有延迟** → `wait_for` 等关键元素，或 `while` 轮询。
5. **页面有公告弹窗拦住操作** → 前面加一步 `if: "exists(...)"` + `then: [click_text: 关闭]`。
6. **表格多行都要处理** → `for_each` + `rows: "table tr"`。
7. **不确定能不能点到** → `optional: true`（找不到不算失败）；步骤整体容错用
   `continue_on_error: true`。
8. **需要在页面间来回切** → `switch_page` 按 URL 片段切换。

---

## 七、调试建议

1. 先 `--validate` 过语法、动作名与常见参数；
2. `settings.max_tasks: 1` 单条试跑，再放大；
3. 失败看 `logs/run_日期.log`，同时看 `shots/` 里 `*_异常_*.png` 截图；
4. 页面结构看不清：菜单 1 `probe` —— 回车打印元素清单与选择器建议，
   `t=关键词` 现场验证文字匹配，`w=页面.yaml` 保存含步骤草稿的 YAML；
5. 不想手写步骤：菜单 2 `record` 在浏览器里点一遍，自动生成步骤再手工裁剪；
6. 没把握的选择器先用 `expect_visible` / `exists()` / `count()` 探测；
7. `probe` 的 `suggested_steps` 只是"元素到动作"的草稿，请按业务流程裁剪与排序。

---

## 八、YAML 语法注意

内嵌解析器支持的是 YAML 常用子集（与 PyYAML 做过 8000+ 例对拍）：
缩进映射/序列、行内 `[a,b]`/`{k: v}`、引号串、多行块标量 `|` `>`、注释、中文。

- **缩进用空格，不要用 Tab**；同一层缩进必须一致；
- 冒号后要有空格：`uses: click`；
- 含 `:` `#` `{` `}` 等特殊字符的值请加引号，如 `message: "共 3 条 # 备注"`；
- 不支持：锚点 `&`/别名 `*`、多文档 `---`、显式标签 `!!`。
