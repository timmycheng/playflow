# tool_kit 使用说明 —— 内网平台任务自动化工具

一套基于 Playwright + 系统 Chrome 的内网任务自动化工具。**两种部署形态任选**：

- **方案 A（默认）**：外网打包成 exe，拷贝到内网双击运行，目标机什么都不用装；
- **方案 B（备用）**：拷贝 `离线安装包.zip`（官方 Python 安装器 + 离线 wheel 依赖），
  内网安装 Python 后源码运行 `tool_kit.py`。适合内网杀软拦截 PyInstaller exe 的情况。
  两种方案代码、config.json 完全一致，用法无差别。

> **页面复杂？用 YAML 编排。** 除了固定流程的 `run` 模式，工具还支持**类 GitHub Actions 的
> `workflow` 模式**：用 `workflow.yaml` 把任务写成可自由组合的步骤
> （`goto / click_text / fill / pick_radio / download / if / for_each / while ...`），
> 复杂页面只改 YAML、不改代码。详见《工作流参考.md》及 `workflow.yaml`、`workflow.advanced.yaml`。
> YAML 解析器内嵌在工具中（纯标准库），**无需额外安装任何依赖**，离线可用。
> **workflow 模式不需要 config.json**：登录与设置写进 workflow.yaml 即可直接运行；
> config.json 只是可选兜底（没写到的项才取它的值）。

## 〇、方案 B：离线安装包（源码运行）

`离线安装包.zip`（约 66MB）内容：`python-3.14.3-amd64.exe`（官方安装器，
签名可在内网右键→属性→数字签名核验）、`wheels/`（playwright+requests 及全部依赖，
与 Python 3.14 / win_amd64 严格匹配）、工具四件套、`安装.bat` 一键脚本。

内网操作（要求 Win10/11 64 位 + 已装 Chrome，当前用户级安装无需管理员）：

1. 解压 `离线安装包.zip` 到任意目录，双击 `安装.bat`（三步：装 Python → 离线装依赖 → 试驱动 Chrome）；
2. 双击 `启动tool_kit.bat` 运行工具；

或手敲命令：

```bat
python-3.14.3-amd64.exe /quiet InstallAllUsers=0 PrependPath=1
:: 重开 cmd 后：
python -m pip install --no-index --find-links=wheels playwright requests
python tool_kit.py
```

`--no-index` 保证 pip 全程不访问网络。详细说明见包内《离线安装说明.md》。

## 一、文件清单

| 文件 | 用途 | 是否拷入内网 |
|---|---|---|
| `tool_kit.exe`（打包产物） | 主工具，双击运行（方案 A） | ✅ 必带 |
| `config.json` | 基础配置（记事本可编辑，UTF-8） | ✅ 必带（workflow 模式可省略） |
| `workflow.yaml` / `workflow.advanced.yaml` | YAML 工作流（复杂页面推荐，模板可改） | ✅ 推荐 |
| `工作流参考.md` | YAML 工作流完整说明（字段、动作、条件、示例） | 建议 |
| `离线安装包.zip` | 方案 B：Python + 依赖离线安装包 | 二选一 |
| `mock_platform.py` | 本地模拟平台，外网彩排用 | ❌ 不需要 |
| `selftest.py` | mock 平台自动验收脚本（彩排辅助） | ❌ 不需要 |
| `使用说明.md` | 本文档 | 建议 |

运行后自动生成的目录/文件（都在 exe 同目录下）：

| 产物 | 说明 |
|---|---|
| `state.json`（+ `state.meta.json`） | 登录态及其来源站点。有效期内重复运行**不再触碰 UKey**；来源不匹配会自动重新登录 |
| `logs/run_日期.log` | 全程日志：动作、任务号、结果、异常堆栈 |
| `shots/序号_阶段_时间戳.png` | 每个关键动作前后的截图 |
| `附件/类别名/任务号_文件名` | 下载的附件（可由配置关闭） |
| `shots/probe_*.yaml`、`shots/record_*.yaml` | 探测 / 录制自动生成的 YAML 草稿（含 suggested_steps），可直接粘贴到工作流 |

## 二、外网打包步骤（开发机，需可装 Python 包）

```bat
:: 1. 安装 Python 3.10+ 后安装依赖（无需 playwright install 下载浏览器）
pip install playwright requests pyinstaller

:: 2. 在源码目录执行规范打包命令
pyinstaller -F tool_kit.py --collect-all playwright

:: 3. 产物在 dist\tool_kit.exe，把它和 workflow.yaml（推荐）一起带走；
      若还要用 run 模式，再带上 config.json
```

> 说明：工具通过 `channel="chrome"` 驱动系统已安装的 Chrome，
> **不依赖 Playwright 自带的 Chromium**，所以内网机器不用下载任何浏览器。

### 外网彩排（强烈建议，出门前完成）

```bat
:: 窗口1：启动本地模拟平台（模拟 SSO 登录 + UKey + 双类别任务 + iframe + confirm）
python mock_platform.py

:: 窗口2：跑自动验收（全绿即彩排通过；覆盖 run/workflow/probe/record）
python selftest.py

:: 或手工彩排：python tool_kit.py → 选 6（demo），账密 admin/123456，
::   UKey 处点"点击模拟UKey验证"后回车
:: 也可以：python tool_kit.py --workflow workflow.demo.yaml
```

## 三、内网部署步骤

推荐 **YAML 优先**（页面复杂只改 YAML，不改代码；不需要 config.json）：

1. 拷贝 `tool_kit.exe` + `workflow.yaml` 到内网虚拟机同一目录（如 `D:\toolkit\`）。
2. 双击 `tool_kit.exe`，选 `1. probe`：完成登录/UKey 后，在浏览器里浏览到列表页、详情页，
   每停一页按回车，控制台会打印按钮文字、输入框、单选框和选择器建议；
   输入 `w=页面.yaml` 可把扫描结果存成含 `suggested_steps` 的草稿。
   （也可以选 `2. record`，在浏览器里手动点一遍，自动生成 YAML 步骤。）
3. 把生成的步骤粘进 `workflow.yaml` 的 `steps:` / `tasks[].steps:`，填上真实 URL 与账号：
   - 先 `tool_kit.exe --workflow workflow.yaml --validate` 校验语法和动作名；
   - 保持 `settings.max_tasks: 1` **单条试跑**，检查附件、radio 决策、提交结果；
   - 试跑无误后把 `max_tasks` 调大（如 `999`）批量执行。
4. 若坚持旧的固定流程，也可以继续用 `config.json` + `5. run`（字段见下一节）。

## 四、config.json 字段说明

JSON 本身不能写注释，逐项说明如下（带 ⭐ 为必填）：

| 字段 | 说明 |
|---|---|
| ⭐ `base_url` | 业务平台首页地址。工具用它验证登录态是否有效 |
| ⭐ `sso_login_url` | SSO 账密登录页地址 |
| `sso_username` / `sso_password` | 登录账号密码；留空则运行时提示人工在浏览器中输入 |
| `login_username_selector` / `login_password_selector` | 登录页输入框选择器，留空用智能默认（第一个可见文本框/密码框） |
| `login_button_keywords` | 登录按钮文字关键词，默认 `["登录","登陆","Login"]` |
| `channel` | 浏览器渠道，默认 `chrome`（驱动系统 Chrome） |
| `headless` | 是否无头模式。**建议保持 false**：UKey 与人工环节需要可见窗口 |
| ⭐ `categories` | 任务类别数组。`name`：类别名（附件/截图/汇总用它）；`url`：任务列表页地址；`row_selector`/`link_selector`：留空用智能默认（第一个含链接的行 / 行内第一个 `<a>`），inspect 确认后可填精确选择器覆盖 |
| `radio_rules` | 任务类型 → 单选框映射，如 `{"if_type_contains": "报修", "then_pick": "同意"}`。类型素材 = 列表行文本 + 详情页标题/正文前 500 字，**按顺序逐条匹配** |
| `default_radio` | 规则都没命中时选的单选框文字 |
| `sign_keywords` | 签收类按钮关键词，默认 `["签收","受理","确认"]` |
| `submit_keywords` | 提交类按钮关键词，默认 `["提交","确定","保存"]` |
| `download_attachments` | 是否下载附件，`true`/`false` |
| `attachment_dir` | 附件根目录名，默认 `附件` |
| `max_tasks` | 每个类别最多处理条数。**先用 1 试跑，再改大** |
| `task_delay_seconds` | 每条任务之间的随机等待区间（秒），默认 `[1,3]`，模拟人工节奏 |

所有关键词类配置都支持中文文字匹配，**自动容错按钮文字中的空格/换行**（如“提 交”）。

## 五、各模式的使用时机

菜单：

| 菜单 | 模式 | 什么时候用 |
|---|---|---|
| 1 | `probe` | **第一次接触真实页面必用**。合并了原 inspect + debug：登录后人工浏览，回车打印元素清单与选择器建议；`t=签收` 验证文字匹配；`w=页面.yaml` 保存 YAML 草稿 |
| 2 | `record` | 不想手写步骤时：在浏览器里点一遍（点击/填写/下拉/上传），自动生成 YAML 步骤并写文件；密码自动替换为 `{{ env.password }}`，不落明文 |
| 3 | `workflow` | **首选执行方式**。读 `workflow.yaml`，按 YAML 步骤自由编排（条件/循环/iframe 定向/多标签页/接口），只改 YAML 不改代码；config.json 可省略 |
| 4 | `validate` | 改完 YAML 先校验：语法、动作名、常见参数、tasks.mode，避免到内网才发现写错 |
| 5 | `run` | 兼容旧用法：读 `config.json` 固定流程（签收→选radio→下载→提交） |
| 6 | `demo` | 本地彩排：连本机 `mock_platform.py` 跑一遍标准流程（外网开发机上用） |

命令行等价用法：

```bat
tool_kit --workflow 我的流程.yaml             :: 执行
tool_kit --workflow 我的流程.yaml --validate  :: 只校验
tool_kit --probe [--workflow 我的流程.yaml]   :: 交互探测（复用该 YAML 的登录配置）
tool_kit --record out.yaml [--url 网址]       :: 录制并生成 out.yaml
```

YAML 工作流的完整字段、动作速查、条件写法见《工作流参考.md》；
`workflow.yaml` 是通用模板，`workflow.advanced.yaml` 是高级手法示例，
`workflow.demo.yaml` 是对着 mock 平台的彩排示例。

## 六、登录与 UKey 的半自动机制

1. 每次启动先复用 `state.json` 并访问 `base_url` 验证：没被踢回登录页 = 登录态有效，
   **全程不碰 UKey**。
2. 登录态缺失/过期：自动打开 SSO 页、填账密、点登录 → **暂停**，控制台提示
   “请在弹窗中完成 UKey 验证，完成后按回车继续”——UKey 弹窗是浏览器外的独立驱动程序窗口，
   无法自动化，必须人工处理。
3. 按回车后工具自动回到平台首页校验，成功则把登录态写入 `state.json`。
4. 批量运行中途若被踢回登录页：同样暂停等人工完成 UKey，回车后**自动恢复队列继续跑**。

## 七、可靠性与自证（内网无外部支持）

- 每个关键动作（进列表/点链接/签收/选radio/下载/提交）前后截图存 `shots/`；
- 全程写 `logs/run_日期.log`，含每个任务编号、结果与异常堆栈；
- 单条任务失败：截图 → 记录任务号 → 关标签 → **跳过继续下一条**，不中断整体；
- 致命失败（登录态失效、列表打不开）：截图 + 保存现场 HTML 到 `shots/` + 中文提示；
- 结束打印汇总：各类别成功/失败/跳过数量与失败任务号列表；
- 单任务之间随机等待 1~3 秒（可配置），模拟人工节奏。

## 八、常见问题（FAQ）

**Q1：提示“浏览器启动失败（channel=chrome）”？**
目标机没装 Chrome 或版本过旧。工具只驱动系统 Chrome，不自带浏览器。

**Q2：工作流报“找不到按钮 / 单选框”？**
先跑 `1. probe` 看该页面真实的按钮文字与选择器建议，再把文字补进
`click_text` 的 `text` 列表 / `click` 的 selector；用 `t=关键词` 可现场验证。
文字匹配已自动忽略空格，所以“提 交”用“提交”就能命中。

**Q3：列表页在 iframe 里找不到任务行？**
工具本身会遍历所有 frame，一般无需处理。若行结构特殊，用 probe 看清后给对应
task 加 `row_selector` / `link_selector` 精确选择器；列表在 iframe 内也可用
`frame: "#1"` 定向。

**Q4：record 录出来的步骤不对 / 少了几步？**
录制捕获的是点击、填写、下拉、勾选、上传、回车；地址栏跳转、拖拽等不会录。
建议 `2. record` 粗录 + `1. probe` 核对，再手工调整；动态出现的元素给步骤加
`optional: true` 或 `wait_for` 更稳。

**Q5：控制台中文乱码？**
exe 内部已做 GBK 兜底不会崩溃；如外显乱码可在 cmd 中先执行 `chcp 65001`。

**Q6：附件没下载？**
检查 `download_attachments` 是否为 true；详情页附件链接文字需包含“附件/下载”
或带 `download` 属性。

**Q7：想重新完整登录（更换账号等）？**
关闭工具，删除 exe 同目录的 `state.json`（若有 `state.json.meta.json` 一并删除），
重新运行即走完整“账密 + UKey”流程。登录态是**按站点绑定**的：别的平台/别的流程的
state 不会被误用；多个流程想各留一份登录态，给流程配不同的 `login.state_file`。

**Q8：双击 exe 被 SmartScreen/杀软拦截？**
选“仍要运行”；内网环境可让管理员对 exe 目录加白名单。若不便加白，改用**方案 B**
（`离线安装包.zip` 源码运行），Python 官方安装器自带数字签名，通常不触发拦截。

## 九、安全边界声明

本工具代码中**不含**任何在线下载、检查更新、数据上报逻辑；仅访问 `config.json`
或 `workflow.yaml` 中显式配置的内网地址。日志、截图、附件全部保存在本地。
